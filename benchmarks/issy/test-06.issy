state int x

formula {
    assume [x = 0]
    assert [true]
}

game Safety from l0 {
    loc l0 1
    loc l1 0
    
    from l0 to l0 with [x <= 10] && [x' = 1]
    from l0 to l1 with [x > 10] && [x' = 1]
    from l1 to l1 with [x' = 1]
}
