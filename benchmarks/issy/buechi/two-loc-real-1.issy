state int x
state int y

game Buechi from l1{

    loc l1 1
    loc l2 1

    from l1 to l2 with [x' = x - 1] && [y' = y + 1]
 
    from l2 to l1 with ([x > 0] && [y < 2])   && [x' = x] && [y' = y]
    from l2 to l2 with ([x <= 0] || [y >= 2]) && [x' = x] && [y' = y]

}
