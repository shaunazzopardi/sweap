var Int x
var Int y

inp Int i_x

SPECIFICATION

assume {
  G(gte x i0());
  G(gte y i0());
}

guarantee {
G((gt x i0() && gt y i0()) -> ([x <- sub x i1()] && [y <- y]));
G((eq x i0() && gt y i0()) -> ([x <- i_x] && [y <- sub y i1()]));
}

guarantee { 
  F (eq y i0());
}
