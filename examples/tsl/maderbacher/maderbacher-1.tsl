//-- State: counter

assume {
  eq counter i0();
}

guarantee {
  F (le counter i0());
}

always guarantee {
  ge counter i0();  /* INV */
  le counter i1(); /* INV */

  [ counter <- add counter i1() ] || [ counter <- sub counter i1() ];

}
