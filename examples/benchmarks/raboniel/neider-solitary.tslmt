#LIA
//-- State: x, y

assume {
  lte x int3();
  gte x int0();
}

always guarantee {
  lte x int3(); /* INV */
  gte x int0(); /* INV */

  ([x <- x] || [x <- add x int1()] || [x <- sub x int1()]);
  ([y <- y] || [y <- add y int1()] || [y <- sub y int1()]);
}
