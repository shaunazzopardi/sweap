from setuptools import setup, find_packages

setup(
    name='new-symtri',
    version='',
    packages=find_packages(include=['parsing', 'programs', 'prop_lang']),
    entry_points={
        'runner' : {
            ''
        }
    }
    url='',
    license='',
    author='shauna',
    author_email='',
    description=''
)
